# ---- Secrets: NEVER commit ----
.env
*.env
# Certificate private keys / key material (Graph app cert lives OUTSIDE the repo)
*.pem
*.key
*.pfx
*.p12

# ---- Virtual environment ----
.venv/

# ---- Runtime state, logs, caches ----
.state/
logs/
.canoe_last_run.json
__pycache__/
*.pyc

# ---- Downloaded documents & archives: NEVER commit client/financial data ----
archive/
Private Fund Reporting/
../Private Fund Reporting/
*.pdf

# ---- Statement tracker artifacts (contain fund names; live beside the archive) ----
_statement_tracker/
statement_metadata_cache.json
statement_status.csv
statement_received_log.csv
statement_schedule.csv
statement_schedule.xlsx
Statement Tracker.xlsx

# ---- Reclassify / routing / activity artifacts (contain file names) ----
_reclassify_*.json
_reclassify_*.csv
_route_*.json
_route_*.csv
_download_activity*.csv

# NOTE: run_history.csv is intentionally NOT ignored — it holds run timestamps + counts only (no file names).

# ---- macOS / editors ----
.DS_Store
.idea/
.vscode/
